#include <iostream>
#include <map>
#include <vector>

using namespace std;

class Solution {
public:
    int coinChange(vector<int>& coins, int amount) {

    }
};

int main()
{
  Solution s;
  vector<int> v{1, 6, 3, 2, 5};
  int result = s.coinChange(v, 11);

  return 0;
}
